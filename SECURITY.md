# 安全政策 / Security Policy

## 报告安全漏洞 / Reporting a vulnerability

SoLab 是一个本地优先的 LLM 客户端与 APK 逆向工具链，处理用户主动选择的 APK 文件及用户自己的 API 密钥。如发现安全问题（例如：未授权的文件访问、密钥泄漏、远程代码执行、缓冲区溢出等），请**不要**公开提交 Issue。

请通过以下方式私下报告：

- 在公开仓库启用后，使用 GitHub Security Advisory；在此之前，请通过发布渠道向维护者索取私下联系途径。不要在 Issue、Pull Request 或日志中提交密钥、完整 APK 或可利用细节；
- 说明：受影响版本、复现步骤、影响范围、可行修复建议（如已知）。

## 安全使用须知 / Security notes for users

- 应用具备 **APK 逆向/修改**能力，修改并安装第三方 APK 可能影响设备安全与稳定性，请在受控环境中使用。
- 应用在局域网内提供可选 MCP 服务（默认端口 8800），仅在你自愿开启时对外暴露；请勿在不受信任的网络中开启。
- 第三方模型服务的可用性、费用、鉴权方式和数据政策由对应提供方决定；接入时只使用你有权使用的账号或密钥。API 密钥保存在本机应用数据中。
- MCP 服务仅应在受信任网络中开启；如需通过局域网访问，应设置访问令牌并避免暴露到公网。
- 构建二进制/签名：请从可复现的源码、依赖与发布记录构建，并仅从可信发布渠道获取 APK。

## 支持范围 / Supported

当前仅维护 `master` 分支的最新版本。安全修复将以新版本形式发布。
